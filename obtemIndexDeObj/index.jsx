const getIndex = () => {

    const keys = Object.keys(playerAnalysis);
    console.log(keys)
    if (index >= 0 && index < keys.length) {
      const key = keys[index];
      const data = playerAnalysis[key];
    //   console.log(data);

    }

  
    
}
